// A 'tsconfig.json' file specifies TypeScript settings for its folder.
// Visit https://aka.ms/tsconfig.json to read more about them.
//
// You don't need to understand this file to learn TypeScript!
// Particularly important ones are documented here for fun.
{
  "compilerOptions": {
    "target": "es5",

    // This specifies what built-in global variables are available:
    // * "dom" says we're in a browser so things like HTML elements exist
    // * "esnext" says we can use all the latest fancy JavaScript features
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],

    "allowJs": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,

    // Note that this file doesn't enable stricter checks such as noImplicitAny or strictNullChecks.
    // They're very useful but create-react-app uses this file to figure out compilation settings.
    // We don't want to block rendering the page for forgetting to fix a type declaration.
    // "strict": true,

    "forceConsistentCasingInFileNames": true,
    "noFallthroughCasesInSwitch": true,

    // We use the nice import & export syntax (ES modules), not AMD or CommonJS.
    "module": "esnext",

    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": [
    "src"
  ]
}
